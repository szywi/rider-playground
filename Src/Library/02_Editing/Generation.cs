namespace Library._02_Editing;

// ctor
// ctorp
// Generate (Alt+Insert)
// Generate file (Alt+Insert)
public class Generation
{
    public bool Foo { get; set; }
    public string Bar { get; set; }

    // Foreach
    public void DoSomething(IEnumerable<int> enumerable)
    {
    }
}