namespace Library.Editing;

// .var postfix
// .foreach postfix
// .if postfix
// .yield postfix
public sealed class PostFixTemplates
{
    // public IEnumerable<int> WhereEven(IEnumerable<int> enumerable)
    // {
    //     foreach (var number in enumerable)
    //     {
    //         if (number % 2 == 0)
    //         {
    //             yield return number;
    //         }
    //     }
    // }
}